<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirige todo hacia public (excepto peticiones directas a public)
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
